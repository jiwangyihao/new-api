# AI API 网关

本上下文统一描述用户购买套餐、获得请求消费 Credit，以及系统据此授予服务权益时使用的领域语言。

## 套餐与 Credit

**计时套餐**：
在有限有效期内授予 Credit 和服务权益的套餐；Basic、Standard、Pro、Max 是计时模式下的真实套餐档位。
_避免使用_：月卡（计时套餐不一定按月配置）

**Credit 余额套餐**：
全系统唯一的专用套餐，也是每个用户至多持有一份的不限时、不重置 Credit 权益；它不直接定价或计时销售，只统一定义并发、队列、BusinessCode 等非模型服务限制，不限时购买和符合条件的转换都向其实例累加 Credit。任何套餐都不限制模型范围，遗留 `model_limits` 数据不参与权益判断。
_避免使用_：余额档位、不限时套餐档位

**Credit 余额**：
Credit 余额套餐中尚可用于请求消费的 Credit 数量，与用于支付购买订单的人民币账户余额是两个不同概念。
_避免使用_：账户余额、钱包余额、人民币余额

**结算欠额**：
已开始请求在最终结算时超出 Credit 余额而形成的待抵扣 Credit；它不允许继续发起额度不足的新请求，后续充值或转换所得 Credit 必须先抵扣该欠额。
_避免使用_：透支额度、信用额度

**不限时购买选项**：
用户在不限时模式下从现有计时套餐中选择的充值档位；它完整复用所选计时套餐的售价和每周期 Credit，因此 Basic、Standard、Pro、Max 只表达既有的购买数量与折扣，不决定用户持有哪一种不限时权益或服务限制。
_避免使用_：不限时套餐档位、永久套餐等级

**不限时购买资格**：
计时套餐是否可以作为不限时购买选项的显式配置；只有正常付费且管理员已开启该资格的计时套餐，才可向 Credit 余额套餐充值。

**计时转换资格**：
月度计时套餐的既有权益是否允许折算为 Credit 余额的独立配置；它与不限时购买资格互不隐含，试用套餐和每月邀请权益即使误配置也始终不可转换。

**套餐转换**：
将符合条件的计时套餐不可逆地折算并并入 Credit 余额套餐；只允许从计时套餐转换到 Credit 余额套餐。
_避免使用_：套餐切换（容易暗示可以双向切换）

**转换冷却期**：
计时套餐每次新购或续期后暂不可转换的 24 小时时段，用于避免刚获得的当前周期 Credit 与剩余期限被立即重复折算。

**转换报价**：
根据计时套餐当前剩余期限、最近一次成功授予或续期的履约 Credit 快照与权益实际剩余 Credit 持续更新的预计转换结果；历史权益没有快照时，使用套餐当前月 Credit，用户确认表示接受提交时重新计算的最新结果，而不是锁定先前显示的数值。

**转换宽限期**：
计时套餐到期后的滚动 336 小时时段；确认转换时，到期时刻恰好等于当前时刻减 336 小时仍符合资格，超过该边界后不再具备转换资格。

**可转换权益**：
来源为付费订单、兑换码、管理员赠送或补偿，且仍有效或处于转换宽限期内的月度计时套餐权益；试用和每月邀请权益不属于可转换权益。

**逐项转换**：
用户每次选择一份可转换权益，将其单独折算并并入 Credit 余额套餐；其他计时套餐继续保留，之后仍可分别转换。
_避免使用_：全部转换、整户转换

**计时套餐续期**：
用户重复购买同一份仍有效、允许续期的非试用计时套餐时延长其既有权益期限，而不是创建第二份有效权益；试用套餐不可续期，每月邀请权益不与普通计时权益归并。同档旧权益已经到期时，新购会创建新的有效权益，仍在转换宽限期内的旧权益作为不可扣费的逐项转换项保留。

**活动套餐**：
用户明确选定、供“单一活动套餐”使用或供“活动套餐自动回退”首先尝试的一份套餐权益；若该权益被转换，Credit 余额套餐接替其活动身份；若该权益过期或停用，系统改选最早到期的其他计时套餐，没有其他计时套餐时改选 Credit 余额套餐；转换其他权益不改变当前选择。

**扣费策略**：
用户在同时持有计时套餐和 Credit 余额套餐时选择的权益来源规则；可选“单一活动套餐”“活动套餐自动回退”或“计时套餐固定优先”，默认使用“单一活动套餐”。
_避免使用_：套餐模式、购买模式

**单一活动套餐**：
一次请求只从活动套餐权益扣费；活动套餐过期或停用时允许按活动套餐规则改选，但 Credit 不足、模型权限不足、并发饱和或请求执行失败时不自动改用其他权益，也不跨权益拆分 Credit。

**活动套餐自动回退**：
一次请求先尝试用户选定的活动套餐，仅在其已失效、停用或预估 Credit 不足时，才按到期时间从近到远尝试其他计时套餐，最后尝试 Credit 余额套餐；模型权限不足、并发饱和或请求执行失败不触发回退，最终只由一个权益承担完整请求。

**计时套餐固定优先**：
一次请求忽略活动套餐，按到期时间从近到远选择可承担完整请求的计时套餐，没有此类套餐时才使用 Credit 余额套餐；模型权限不足、并发饱和或请求执行失败不触发改选，且不跨权益拆分 Credit。


**人民币账户余额**：
用户可用于支付套餐订单的货币金额，不直接承担请求消费 Credit 的扣减。
_避免使用_：Credit 余额

**运营剩余价值**：
在指定快照时点，对尚可交付的有价套餐权益和 Credit 余额按其来源成本基础估算的剩余服务价值；用于运营分析，不代表会计递延收入、现金负债或可退款金额。
_避免使用_：递延收入、未履约负债、实收余额

**Credit 成本基础**：
某次 Credit 入账在运营估值中的货币价值及币种；它描述该批 Credit 的价值来源，不等同于用户可退款金额。

**移动加权平均单位价值**：
同一用户的 Credit 合并后，以尚未摊销的确值与估值成本基础总额除以全部可用 Credit（包括成本基础未知的 Credit）得到单位运营价值；后续请求消费按操作前的平均值同步减少运营剩余价值，并按比例减少估值金额与未知 Credit 披露。
_避免使用_：FIFO 成本、逐订单剩余价值

**可估值 Credit 入账**：
具有明确 Credit 成本基础的余额入账；购买采用充值档位的标价快照而非订单实际支付金额，兑换采用兑换时充值档位标价，转换采用转换确认时固化的充值档位单位价值，管理员增加必须选择充值档位并按比例计价。入账先抵扣结算欠额时，只有形成净可用 Credit 的部分进入运营剩余价值。

**Credit 估值币种**：
全局 Credit 余额套餐声明的唯一运营估值计量币种；来源币种不同的可估值 Credit 入账按入账时汇率换算并固化，后续汇率变化不改写既有成本基础。
_避免使用_：展示币种、动态换算币种

**Credit 估值置信度**：
Credit 成本基础的可证明程度，分为确值、估值和未知；运营剩余价值可合并确值与估值，但必须同时披露其中的估值金额及成本基础未知的 Credit 数量。
_避免使用_：全部精确、完整实收

**转换估值快照**：
计时权益转换确认时，以源套餐权威整数标价与转换公式使用的同一份 `credit_basis` 形成单位价值，并按 `full_31_day_blocks × credit_basis + current_remaining_credit` 的毛 Credit 固化成本基础；后续套餐改价不改写该值。它是按运营规则确定的确值，不代表转换时发生了新的收款。

**售后授予**：
管理员为履约纠偏或用户补偿而授予的计时套餐或 Credit；它按所选套餐档位计入运营剩余价值，不属于免费赠送。Credit 售后授予允许任意正数量，并按所选档位的单位 Credit 价值等比例计价。

**权益估值快照**：
计时套餐在购买、兑换或售后授予时固化的套餐档位价格、币种、Credit 数量与估值规则版本；它决定该权益后续运营剩余价值的价格基数，套餐改价不回写既有快照。

**Credit 汇率快照**：
异币种 Credit 入账发生时固化的来源币种、估值币种、换算汇率与换算时间；它只服务运营估值，不改变订单原币种金额。

**Credit 估值状态**：
每份 Credit 余额权益对应的一行物化运营估值投影，保存可用 Credit、确值成本微单位、估值成本微单位、成本未知 Credit、估值币种、规则版本和状态版本；它必须与 `token_limit` / `token_used` 在同一数据库事务中更新，不是另一套可独立变动的钱包余额。

**请求估值扣除快照**：
一次 API 请求预扣或追加结算时，从 Credit 估值状态中实际移除的确值成本、估值成本和成本未知 Credit 份额；请求失败或最终用量减少时按该请求快照原样或同比例恢复，避免期间发生其他入账后按新平均值退款造成价值漂移。

**当前一致快照**：
运营分析通过 `snapshot_at` 协商同一轮接口请求的观察时点；Credit 估值只承诺读取当前物化状态，不支持任意历史时点回放。若目标时点早于状态最近更新时间，响应必须明确标记 `current_only`，不得伪装成历史值。

**计时权益估值授予记录**：
一次计时套餐购买、兑换或售后授予对应的不可变运营估值记录，固化来源身份、服务窗口、档位标价、原币种、Credit 与规则版本；同一权益续期会追加记录，不覆盖先前记录。

**估值迁移门禁**：
决定套餐数量写入是否必须与运营估值原子双写的版本化状态；历史迁移验证完成前为非就绪，完成后为 `ready`。强制双写开放后若需回滚，必须先停写并通过维护命令置为 `suspended`，不得让旧镜像继续单独修改 Credit 数量。

## 渠道组与被动可用性观测

**渠道**：
连接上游 AI 服务、承载一组可调用模型的配置单元，同一渠道可归属多个独立渠道组。
_避免_：将渠道与渠道组、路由分组混称。

**渠道组**：
独立维护的具名渠道集合，具有自己的启用状态；同一渠道可以属于多个渠道组，组的启用状态不代表成员渠道已启用或真实请求正常。
_避免_：用路由分组或渠道标签代指渠道组。

**请求最终结果**：
同一次用户调用经历重试后形成的最终成功或失败结果；重试中的失败尝试不作为额外的失败请求计数。
_避免_：将单次上游尝试结果当作用户请求的最终结果。

**请求成功率**：
指定渠道组或组内模型在观测时段内，最终成功的用户请求占纳入统计的用户请求的比例，每次用户请求只计一次。
_避免_：上游尝试成功率、按时间计算的在线率。

**有效观测请求**：
纳入渠道组或组内模型可用性统计的用户请求，排除明确由用户自身凭据、额度或请求参数问题导致的失败；这些排除项既不计失败，也不进入成功率分母。
_避免_：将全部请求日志直接作为成功率分母。

**服务侧失败**：
由渠道配置错误、上游额度耗尽、服务限流、超时或服务异常等服务侧原因造成的请求最终失败，计入可用性统计。
_避免_：仅凭 HTTP 状态码将服务侧失败归为用户侧问题。

**客户端单独中断**：
日志明确记录客户端断开、且没有已知服务侧失败证据的未完成请求，不计成功或失败，也不进入成功率分母；客户端断开不能证明用户主动取消，不能覆盖已经发生的服务侧失败。
_避免_：将所有流式中断计为服务故障，或将伴随客户端断开的服务侧失败一律排除。

**近期观测状态**：
渠道组或组内模型最近 15 分钟内有效观测请求所反映的服务表现；没有有效请求时为未知，保留最后观测时间，不沿用旧的正常或异常状态。
_避免_：将无流量视为故障，或将历史成功视为当前存活保证。

**首响应延迟**：
成功请求从开始处理到首次响应的耗时，汇总时取具有有效计时数据的成功请求的中位数；缺少计时数据不代表零延迟，延迟不改变按成功率确定的健康分档。
_避免_：将首响应延迟等同于完整生成耗时，或未经区分就称为首文本 token 延迟。

**健康分档**：
在统计覆盖完整的前提下，依据有效观测请求的成功率划分服务表现：成功率 ≥99% 为正常，≥95% 且 <99% 为波动，<95% 为异常；无有效样本为未知。近期观测状态与历史时间块采用相同分档，各自使用对应时段的样本，分档不构成 SLA 承诺。
_避免_：将请求成功率分档等同于按时间计算的在线率。

**历史展示归属**：
请求发生时，依据当时的渠道成员关系与展示归属规则确定的渠道组归属；之后调整成员关系不迁移既有请求的历史归属，历史展示仍受当前渠道组可见性约束。
_避免_：用当前成员关系改写过去的组表现，或在缺少可靠归属证据时猜测历史归属。

**最终承接组**：
请求最终成功返回的渠道在请求发生时对应的展示渠道组；全部尝试失败时，取最后实际尝试的渠道对应的展示组。同一次请求仅向该组计入一次最终结果，跨组重试中的其他组不追加成功或失败记录。
_避免_：将其他组接替成功算作最初尝试组的成功，或将一次请求重复计入多个组。

**可归属的调度失败**：
用户调用条件合法、但在实际尝试渠道之前因服务侧原因失败，且请求发生时的可靠信息能唯一确定展示渠道组的请求；向该组计入一次失败。无法唯一归属时不猜测、不重复计入多个组，而应标记统计覆盖不足。
_避免_：因没有实际承接渠道就遗漏可归属的服务侧失败，或把归属不明的失败分摊给多个组。

**历史模型**：
在所选历史范围内有可靠观测记录、但所属渠道组当前已不再提供的模型；仅在当前仍可见的组内单独展示并标注“已停止提供”，保留历史表现，不参与近期异常模型提示。
_避免_：删除模型配置时一并隐去历史故障，或将历史模型误列为当前可调用模型。

**少样本观测**：
对应观测时段内有效请求为 1–19 次的观测，仍按实际请求成功率分档，同时提示“样本较少”；20 次只是提示边界，不代表统计置信保证。
_避免_：将少样本与无有效请求的未知状态混为一谈。

**统计覆盖不足**：
观测时段内存在无法可靠确认请求最终结果或历史展示归属等记录缺口，因而无法据已确认样本判定该时段的整体健康状态；已确认样本的成功率和已知异常仅代表已覆盖部分。
_避免_：将记录不完整等同于流量少或无人调用，或用部分样本的高成功率证明整体正常。

**可见渠道组**：
沿用订阅页面渠道组筛选与归属规则、向已登录用户展示的非默认渠道组；额度提示是否因倍率均为 1 而省略，不改变可用性面板中的组可见性。
_避免_：将额度提示省略规则当作渠道组的可见性限制。

**未观测模型**：
渠道组当前提供、但在对应观测时段内没有有效请求样本的模型；它仍属于该组的模型目录，缺少样本不代表模型不可调用或服务正常。
_避免_：将未观测模型当作不支持的模型，或因为没有日志而从模型目录移除。

**组内局部异常**：
同一观测时段内，渠道组中的部分有样本模型出现异常或波动，而组整体请求成功率仍可能达到正常档位的情形；组整体按汇总成功率分档，并另行突出模型异常或波动提示，无样本模型不计为故障。
_避免_：用组整体正常掩盖模型异常，或将组整体分档替换为最差模型的分档。

**默认渠道组**：
用户未为 API Key 选择渠道组时使用的内置渠道组，名为 __default__；其成员可独立配置，与其他具名渠道组当前成员相同不代表两者永久等价。
_避免_：将默认渠道组视为 GPT 组永久不变的别名。

**组内模型**：
某个渠道组提供调用的模型；同名模型可以出现在不同渠道组中。
_避免_：将模型与承载它的具体上游渠道混称。

**路由分组**：
历史版本用于组织模型调用路由的命名分组；当前部署已不再使用这一业务分组体系，不能将遗留字段当作独立渠道组。
_避免_：与独立渠道组、渠道标签混用；尤其不要将 default 路由分组与 __default__ 渠道组混为一谈。

**渠道标签**：
用于归类和管理渠道的标签，不等同于独立渠道组或渠道所属的路由分组。
_避免_：不加区分地称为分组。

**被动可用性观测**：
依据真实业务请求留下的日志判断服务表现的观测方式，不额外发送测活请求；无请求样本本身不能证明服务正常或故障。
_避免_：主动探测、定时测活、实时存活保证。
